import React from "react";

function FloorTables(props) {
  return (
    <div>
      <span className="group-header">in progress</span>
      {props.list}
    </div>
  );
}

export default FloorTables;
